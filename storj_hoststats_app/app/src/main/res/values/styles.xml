<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="ListView" parent="Base.Widget.AppCompat.ListView">
        <item name="android:background">@android:color/white</item>
        <item name="android:divider">@color/light_grey</item>
        <item name="android:dividerHeight">2.0sp</item>
    </style>

    <style name="ListView.NodeId" parent="AppTheme">
        <item name="android:layout_marginLeft">10sp</item>
    </style>

    <style name="ListView.NodeStats">
        <item name="android:layout_marginLeft">10sp</item>
    </style>

</resources>
